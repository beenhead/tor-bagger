server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Static frontend is a single page; anything unknown falls back to it so
    # password-reset links like /?reset_token=... always resolve.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
